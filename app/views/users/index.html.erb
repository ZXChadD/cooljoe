<div class="index">
  <head>
    <title>Bootstrap Example</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  </head>

  <body>
    <div class="container">
      <ul class="nav nav-tabs">
        <li class="active"><a data-toggle="tab" href="#menu1">New Jobs</a></li>
        <li><a data-toggle="tab" href="#menu2">Current Jobs</a></li>
        <li><a data-toggle="tab" href="#menu3">Job History</a></li>
        <li><a data-toggle="tab" href="#menu4">Invoices</a></li>
      </ul>

      <div class="tab-content">
        <div id="menu1" class="tab-pane fade in active">
          <h3>New Jobs</h3>
          <div class="panel panel-default">
            <table class="table">
              <tr>
                <td>#</td>
                <td>Date</td>
                <td>Summary</td>
                <td>Status</td>
                <td>Response</td>
                <td>Edit</td>
              </tr>
              <% @new_joblistings.each_with_index do |new_joblisting, index| %>
                <tr>
                  <td><%= index + 1 %></td>
                  <td><%= new_joblisting.created_at.strftime("%F") %></td>
                  <td><%= link_to "#{new_joblisting.description}", joblisting_path(new_joblisting) %></td>
                  <td><%= new_joblisting.status %></td>
                  <td><%= link_to 'View Providers', result_path(job_id: new_joblisting.id) %></td>
                  <td><%= link_to 'Edit', edit_joblisting_path(new_joblisting) %></td>
                </tr>
              <% end %>
            </table>
          </div>
        </div>
        <div id="menu2" class="tab-pane fade">
          <h3>Current Jobs</h3>
          <div class="panel panel-default">
            <table class="table">
              <tr>
                <td>#</td>
                <td>Date</td>
                <td>Summary</td>
                <td>Status</td>
                <td>Electrican name</td>
                <!-- <td>Response</td> -->
              </tr>
              <% @current_joblistings.each_with_index do |current_joblisting, index| %>
                <tr>
                  <td><%= index + 1 %></td>
                  <td><%= current_joblisting.created_at.strftime("%F") %></td>
                  <td><%= link_to "#{current_joblisting.description}", joblisting_path(current_joblisting) %></td>
                  <td><%= current_joblisting.status %></td>
                  <% if current_joblisting.status == 'booked' || current_joblisting.status == 'pending' %>
                    <td><%= link_to "#{Provider.find(current_joblisting.provider_id).firstname}", provider_path(Provider.find(current_joblisting.provider_id)) %></td>
                  <% else %>
                    <td>No provider</td>
                  <% end %>
                  <!-- <% if current_joblisting.status == 'created' %>
                    <td><%= link_to "#{current_joblisting.status}", result_path(job_id: current_joblisting.id) %></td>
                  <% end %> -->
                </tr>
              <% end %>
            </table>
          </div>
        </div>
        <div id="menu3" class="tab-pane fade">
          <h3>Job History</h3>
          <div class="panel panel-default">
            <table class="table">
              <tr>
                <td>#</td>
                <td>Date</td>
                <td>Summary</td>
                <td>Status</td>
                <td>Electrican name</td>
              </tr>
              <% @past_joblistings.each_with_index do |past_joblisting, index| %>
              <tr>
                <td><%= index + 1 %></td>
                <td><%= past_joblisting.created_at.strftime("%F") %></td>
                <td><%= link_to "#{past_joblisting.description}", joblisting_path(past_joblisting) %></td>
                <td><%= past_joblisting.status %></td>
                <td><%= link_to "#{Provider.find(past_joblisting.provider_id).firstname}", provider_path(Provider.find(past_joblisting.provider_id)) %></td>
              </tr>
              <% end %>
            </table>
          </div>
        </div>
        <div id="menu4" class="tab-pane fade">
          <h3>Invoices</h3>
          <div class="panel panel-default">
            <table class="table">
              <tr>
                <td>#</td>
                <td>Date</td>
                <td>Invoice Number</td>
                <td>Status</td>
                <td>Electrican name</td>
              </tr>
              <% @past_joblistings.each_with_index do |past_joblisting, index| %>
              <tr>
                <td><%= index + 1 %></td>
                <td><%= past_joblisting.created_at.strftime("%F") %></td>
                <td><%= link_to "#{past_joblisting.description}", joblisting_path(past_joblisting) %></td>
                <td><%= past_joblisting.status %></td>
                <td><%= link_to "#{Provider.find(past_joblisting.provider_id).firstname}", provider_path(Provider.find(past_joblisting.provider_id)) %></td>
              </tr>
              <% end %>
            </table>
          </div>
        </div>
      </div>
    </div>
  </body>
</div>
